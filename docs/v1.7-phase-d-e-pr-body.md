# PR Draft: v1.7 Phase D/E placeholder AI index and read-only UI visibility

## Summary

This change adds a local placeholder AI index surface for the v1.7 vertical-slice track.

It introduces:

- `chronicle ai-index status`
- `chronicle ai-index vector add`
- `chronicle ai-index vector search`
- `chronicle ai-index graph add-node`
- `chronicle ai-index graph add-edge`
- `chronicle ai-index graph neighbors`
- local file-backed placeholder index storage under `.chronicle/ai_indexes/`
- read-only local UI visibility for placeholder AI index surfaces

It also updates README, CLI reference, architecture, interface contracts, changelog, and progress notes so the repo state matches the implementation.

## Boundary

- No LLM invoked
- No embedding provider invoked
- No vector DB invoked
- No graph DB invoked
- No GraphRAG runtime invoked
- No external services
- Indexes are local file-backed placeholder derived surfaces
- Search result is assistive, not correctness proof
- Primary Chronicle records remain authoritative
- Read-only UI visibility does not add GUI mutation

## Verification

Executed:

```bash
.venv/bin/ruff check src tests
.venv/bin/pytest -q
```

Expected key checks covered:

- ai-index CLI tests
- ai-index JSON shape contract
- read-only UI endpoint tests
- UI smoke coverage
- full regression suite

## Warning classification

- Runtime warning: placeholder AI index does not imply model runtime integration.
- Retrieval warning: placeholder search is not semantic retrieval or correctness proof.
- Storage warning: `.chronicle/ai_indexes/*` is derived helper state, not a primary record.
- UI warning: read-only visibility does not imply review approval automation or GUI mutation.
- Future-integration warning: adapter contracts do not imply Chroma, Qdrant, Neo4j, or GraphRAG runtime adoption.

## RDE review

Preserved: local-first primary-record authority, read-only UI boundary, no-runtime boundary, rebuildable derived-surface philosophy.

Transformed: graph-ready / UI inspection surfaces now extend into placeholder AI index visibility and local adapter contracts.

Supplemented: ai-index CLI, local file-backed placeholder storage, UI endpoints, smoke coverage, and contract documentation.

Unresolved: actual local LLM provider adapters, explicit invocation workflow, GraphRAG pipeline skeleton, and mutation-capable GUI review workflow.

Deviation risks: overstating placeholder search as semantic retrieval, treating derived helper state as authoritative, or reading UI visibility as approval/certification.
