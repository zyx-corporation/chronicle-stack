# Chronicle Stack v1.37.0 Smoke Test

Expected `v1.37.0` contract additions:

- review target-state contract exposes `scope_note_key`
- review target-state contract exposes `resolved_behavior_note_key`
- write-route rendering prefers structured target-state note keys
