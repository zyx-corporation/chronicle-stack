# Chronicle Stack v1.28.0 Smoke Test

Expected `v1.28.0` contract additions:

- `/api/review-queue/<id>` package readiness exposes `counts_summary_key`
- `/api/review-queue/<id>` package readiness exposes `boundary_note_key`
