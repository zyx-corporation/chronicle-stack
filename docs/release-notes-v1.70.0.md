# Release Notes v1.70.0

## Added

- checked-in downstream consumer fixture at `docs/examples/query-engine-handoff-example.json`
- companion guide at `docs/query-engine-handoff-consumer-example.md`
- tests that pin the fixture shape against the runtime handoff model

## Changed

- runtime retrieval docs now point to the downstream consumer example and fixture

## Boundary

- the example fixture remains descriptive and read-only
- Chronicle Stack still does not ship a downstream import adapter or query runtime
