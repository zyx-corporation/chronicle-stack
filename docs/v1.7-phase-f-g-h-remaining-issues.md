# v1.7 Phase F/G/H Remaining Issues

Related: `docs/v1.7-phase-f-g-h-plan.md`, `docs/v1.7-phase-f-g-h-closeout-summary.md`, `docs/v1.7-phase-h-readiness-status.md`, `docs/v1.7-phase-h-auth-ui-design.md`, `docs/v1.7-phase-h-ui-mutation-threat-model.md`

This note turns the currently confirmed remaining work into issue-sized backlog items.

Issue numbers are placeholders until GitHub issues are created.

Current repository-side completion point:

- Phase F now includes explicit manual local invocation, explicit configured-provider execution gating, and persisted pending-review draft outputs across summary jobs and generic runtime artifacts.
- Phase F is not complete as a broader provider/runtime slice.
- Phase G has a meaningful dry-run / handoff skeleton.
- Phase H has read-only observability complete and a gated local write-path available behind explicit enablement.

## Candidate issues

```text
#TBD v1.7: Define local reviewer identity and session boundary for gated GUI mutation
#TBD v1.7: Refine mutation-readiness blockers and enablement criteria
#TBD v1.7: Align blocked-route preview and CLI fallback contracts across all local UI surfaces
#TBD v1.7: Design explicit GUI review write routes and failure contracts
#TBD v1.7: Define browser-triggered rollback and error classification model
#TBD v1.7: Expand configured-provider execution beyond the current text-only HTTP invoke contract
#TBD v1.7: Extend provider-produced output persistence beyond current text-output summary/artifact draft flows
#TBD v1.7: Expand retrieval-plan composition without GraphRAG runtime claims
#TBD v1.7: Refine downstream handoff and package handoff review contracts
```

## Issue breakdown

### #TBD v1.7: Define local reviewer identity and session boundary for gated GUI mutation

Scope:

- define what reviewer identity fields are required when the local mutation gate is enabled
- define how session labels are represented and validated
- clarify what is descriptive metadata versus what is required for mutation acceptance

Out of scope:

- multi-user authentication
- hosted identity providers
- authorization enforcement beyond local single-operator assumptions

### #TBD v1.7: Refine mutation-readiness blockers and enablement criteria

Scope:

- make `mutation_readiness` blockers operationally precise
- document explicit prerequisites for local write enablement
- keep overview, list, and detail readiness wording aligned

Out of scope:

- enabling writes by default
- replacing CLI as the primary write path

### #TBD v1.7: Align blocked-route preview and CLI fallback contracts across all local UI surfaces

Scope:

- keep blocked preview, apply response, recovery command, and follow-up command contracts synchronized
- ensure review queue, summary jobs, and detail views share the same fail-closed vocabulary
- extend smoke coverage only where contract surfaces expand

Out of scope:

- real auth/authz enforcement
- new mutation capability beyond the existing explicit gate

### #TBD v1.7: Design explicit GUI review write routes and failure contracts

Scope:

- define route shapes for approve / reject / request-changes
- define request/response contracts for successful and failed local writes
- define how explicit local enablement is checked server-side

Repository-side progress so far:

- the local UI now exposes the write-route template, expected request fields, action set, and status-code contract summary on `ui_boundary` / `mutation_readiness`
- the current repo still stops short of claiming authenticated or default-on GUI mutation semantics

Out of scope:

- default-on mutation
- unaudited write paths

### #TBD v1.7: Define browser-triggered rollback and error classification model

Scope:

- classify audit-write failure versus Chronicle append failure
- define rollback reporting rules for browser-triggered review actions
- align error classes with CLI/operator recovery guidance

Out of scope:

- proving distributed correctness
- cross-process transactional guarantees beyond the current local boundary

### #TBD v1.7: Expand configured-provider execution beyond the current text-only HTTP invoke contract

Scope:

- extend the current explicit local/manual invocation slice beyond the current text-only HTTP invoke contract
- preserve explicit provider kind / model / invocation mode / external-call flags
- keep execution opt-in, operator-visible, and fail-closed when network/external context contracts are missing

Out of scope:

- hidden background execution
- automatic invocation on record creation

### #TBD v1.7: Extend provider-produced output persistence beyond current text-output summary/artifact draft flows

Scope:

- extend the current runtime-manual/provider-output persistence shape beyond the current summary job and generic text-output artifact flows
- keep generated output derived, reviewable, and non-authoritative until accepted
- preserve provenance, runtime metadata, and external-call flags in persisted records

Out of scope:

- automatic approval
- generated output as primary record

### #TBD v1.7: Expand retrieval-plan composition without GraphRAG runtime claims

Scope:

- deepen retrieval-plan composition beyond the current placeholder skeleton
- preserve explicit dry-run semantics
- keep language and docs clear that this is not yet a GraphRAG engine

Out of scope:

- vector DB runtime
- graph DB runtime
- ranking quality claims

### #TBD v1.7: Refine downstream handoff and package handoff review contracts

Scope:

- improve downstream handoff visibility where it helps operator review
- refine package handoff preview and package review snapshot contracts
- keep handoff outputs clearly read-only or derived until explicitly persisted

Out of scope:

- hidden export automation
- package mutation side effects without explicit operator action

## Recommended sequencing

1. Define local reviewer identity / session boundary
2. Refine mutation-readiness blockers and enablement criteria
3. Align blocked-route preview and CLI fallback contracts
4. Design explicit GUI review write routes and failure contracts
5. Define rollback and error classification model
6. Return to Phase F proper with broader configured-provider execution
7. Extend provider-produced draft persistence beyond summary outputs
8. Expand retrieval-plan composition
9. Refine downstream/package handoff review contracts

## Boundary reminders

- Chronicle JSONL remains primary authority.
- The local UI remains derived.
- Readiness/parity summaries are assistive, not approval authority.
- Local mutation must remain explicit opt-in.
- GraphRAG should not be claimed ahead of real runtime implementation.
