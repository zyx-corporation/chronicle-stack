# Chronicle Stack v1.8.0 Release Remaining Issues

Related: `docs/release-status-v1.8.0.md`, `docs/release-readiness-v1.8.md`, `docs/smoke-test-v1.8.md`, `docs/release-notes-v1.8.0.md`, `docs/adr/0027-local-gui-review-route-contract.md`

This note turns the current `v1.8.0` release-track remainder into issue-sized backlog items.

## Current repository-side completion point

Current `v1.8.0` repository-side progress now includes:

- release status, readiness, smoke, and release-notes entry points
- explicit read-only action-route family visibility
- explicit read-only CLI-equivalent route visibility
- explicit read-only write-route status-code visibility

## Candidate issues

```text
#TBD v1.8: Refine local reviewer/session enforcement boundary wording
#TBD v1.8: Capture final v1.8 smoke and publication evidence
#TBD v1.8: Retire v1.7-centric remaining-issues references from the v1.8 release lane
```

## Issue breakdown

### #TBD v1.8: Refine local reviewer/session enforcement boundary wording

Scope:

- keep reviewer/session proof language aligned across ADR, UI contract surfaces, and release docs
- clarify what remains descriptive contract visibility versus future stronger enforcement
- keep local single-operator boundary wording explicit

Repository-side progress so far:

- `ADR-0026` fixes the current descriptive reviewer/session proof shape
- `ADR-0027` fixes the current `v1.8.0` route-expansion boundary
- UI contract surfaces now expose reviewer/session requirements and related route semantics read-only

Out of scope:

- multi-user-safe authority enforcement
- hosted identity providers

### #TBD v1.8: Capture final v1.8 smoke and publication evidence

Scope:

- run the final `v1.8.0` smoke path at cut time
- capture installer / tag / release evidence
- align final evidence with release-operator docs

Repository-side progress so far:

- the smoke profile now exists for the `v1.8.0` contract-hardening lane
- repository-side full-suite verification continues to pass for the current slice

Out of scope:

- security certification
- correctness proof

### #TBD v1.8: Retire v1.7-centric remaining-issues references from the v1.8 release lane

Scope:

- replace `v1.7`-scoped remaining-issues references where `v1.8` now has dedicated docs
- keep historical references only where they still carry active contract detail
- preserve continuity without keeping the active release lane anchored to the old version name

Repository-side progress so far:

- `v1.8.0` now has its own release status, readiness, smoke, release notes, and remaining-issues notes
- active `v1.8.0` release docs now stand on their own, while older `v1.7` records are being narrowed to historical-context use only

Out of scope:

- deleting historical `v1.7` release records

## Recommended sequencing

1. Refine local reviewer/session enforcement boundary wording
2. Retire `v1.7`-centric remaining-issues references from the active `v1.8` lane
3. Capture final smoke and publication evidence

## Boundary reminders

- Chronicle JSONL remains primary authority.
- The local UI remains derived.
- Local mutation remains explicit opt-in.
- `v1.8.0` is still a local contract-hardening lane, not a hosted mutation release.
