# Release Notes v1.73.0

## Added

- `chronicle package query-engine-bundle` for local downstream handoff bundle generation
- bundle manifest contract for handoff, adapter skeleton, and graph export packaging
- downstream handoff bundle runbook and ADR

## Boundary

- the bundle remains local, read-only, and descriptive only
- Chronicle Stack still does not execute downstream import or hosted query runtimes
