# Chronicle Stack v1.36.0 Smoke Test

Expected `v1.36.0` contract additions:

- review write-route `status_code_contract` exposes `when_key`
- HTML rendering prefers structured status-code reason keys
- fallback `when` prose remains present for read-only inspection
