# 2026-09-15 — Yard所有者スコープ・権限モデルADRの登録

## 依頼と実施

利用者提供の `chronicle-yard-adr-scope-permission-model.md` をADRとして登録する依頼により、
ADR-0109を作成し、ADR一覧へ追加した。main `958f6b3d` を基点とする独立ブランチを使用し、
保留中の文書7件や元の作業場所の未コミット編集は取り込まない。

## 判断と意味差分

提供文書の6つの決定を保持した。UI／daemonのP0未修正という記述はPR #391の統合済み状態へ
更新し、ADR-0108の残留リスクへ接続した。設計の採用は共通認可やSaaSの実装完了を意味せず、
既存JSONLの所有権移行・権限付与・APIフィールドの改名を実行しないことを明記した。
外部ノートや士業側の規約を検証済みとは扱わず、提供文書由来の背景・例として保存した。

## 検証と次の判断

元の6決定との対照、関連リンク、差分範囲、既存pytestを確認する。
実装方式・ロール標準化・grant失効／競合・principalとactor／originの対応・検索投影への
適用は次の設計課題であり、この文書登録では実装しない。CI結果は登録PRに記録する。

検証結果: 提供文書の決定1〜6は原文と完全一致。相対リンクはすべて解決し、差分チェック、
Ruff、全582テストが成功した。今回の登録ではローカルactの再実行とmainへの統合は行わない。
