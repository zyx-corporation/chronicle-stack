# Chronicle Stack v1.27.0 Smoke Test

Run from the repository root:

```bash
chronicle ui-smoke
chronicle ui-smoke --json
```

Expected `v1.27.0` contract additions:

- `/api/runtime-records/<id>` package handoff preview exposes `counts_summary_key`
- `/api/runtime-records/<id>` package handoff preview exposes `boundary_note_key`
