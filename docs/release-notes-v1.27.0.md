# Chronicle Stack v1.27.0 Release Notes

Chronicle Stack `v1.27.0` is a local package-handoff structured-contract release over the published `v1.26.0` baseline.

## Added

- structured `counts_summary_key` for package handoff preview counts
- structured `boundary_note_key` for derived/read-only package handoff wording
- smoke/test coverage for package handoff preview structured fields
