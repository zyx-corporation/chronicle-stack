# Release Status v1.160.0

- lane: ai index graph nodes renderer
- status: implemented
- scope: add a dedicated read-only workspace renderer for `/api/ai-index-graph-nodes` with summary and graph-node table panels
- boundary: this release improves local graph-index presentation only; it does not change graph derivation, external runtime behavior, or mutation semantics
