# Release Status v1.94.0

- lane: read-only federation package preview UI surface
- status: implemented
- scope: add `/api/federation-package-preview` as a query-driven local preview surface for explicit bundle directories
- boundary: Chronicle Stack still does not persist packages, auto-discover directories, transport data, or import from this UI route
