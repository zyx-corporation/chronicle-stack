# Release Status v1.93.0

- lane: federation boundary and consent preflight CLI
- status: implemented
- scope: add `federation boundary check` and `federation consent record` as dedicated preflight and append-only audit surfaces
- boundary: Chronicle Stack still does not auto-create packages, transport data, or perform import from these commands
