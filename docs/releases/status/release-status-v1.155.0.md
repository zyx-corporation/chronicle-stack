# Release Status v1.155.0

- lane: runtime config route renderer
- status: implemented
- scope: add a dedicated read-only route renderer for `/api/runtime-config` with summary and boundary panels
- boundary: this release improves local runtime configuration presentation only; it does not change provider configuration persistence, model invocation, or mutation semantics
