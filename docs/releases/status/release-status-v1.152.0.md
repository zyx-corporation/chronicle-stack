# Release Status v1.152.0

- lane: rde workspace renderer
- status: implemented
- scope: add a dedicated read model and renderer for `/api/rde` with route summary counts and RDE-specific list context
- boundary: this release improves read-only local UI presentation for RDE diff records only; it does not change RDE persistence, artifact authority, or mutation semantics
