# Release Status v1.144.0

- lane: workspace detail affordance geometry
- status: implemented
- scope: improve shared workspace-table detail affordance geometry so JSON and primary detail buttons remain actionable without over-compressing action-heavy rows
- boundary: this release changes read-only derived local UI layout only; it does not change route contracts, payload shape, navigation targets, or mutation semantics
