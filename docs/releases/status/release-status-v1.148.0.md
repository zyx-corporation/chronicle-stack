# Release Status v1.148.0

- lane: contexts workspace renderer
- status: implemented
- scope: add a dedicated read model and renderer for `/api/contexts` with route summary counts and context-specific list context
- boundary: this release improves read-only local UI presentation for context records only; it does not change context persistence, proposal review semantics, or mutation gates
