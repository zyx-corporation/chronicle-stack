# Release Status v1.149.0

- lane: events workspace renderer
- status: implemented
- scope: add a dedicated read model and renderer for `/api/events` with route summary counts and event-specific list context
- boundary: this release improves read-only local UI presentation for Chronicle event records only; it does not change event persistence, artifact authority, or mutation semantics
