# Release Notes v1.155.0

- added a dedicated multi-panel renderer for `/api/runtime-config` so runtime configuration opens as a feature-facing route instead of raw response JSON only
- surfaced runtime config source, provider details, capabilities, warnings, and boundary defaults in the local read-only UI before deeper inspection
- updated operator-facing validation guidance so local UI walkthroughs include runtime config route checks
