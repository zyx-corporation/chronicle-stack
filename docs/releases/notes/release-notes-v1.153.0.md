# Release Notes v1.153.0

- added a dedicated `Proposals Workspace` renderer for `/api/proposals` so proposal records open with a workspace summary rail instead of the generic fallback table
- surfaced proposal list context for review status, latest disposition, target kind, CLI apply guidance, and latest detail navigation before detail is opened
- updated operator-facing validation guidance so local UI walkthroughs include `Proposals Workspace` route checks
