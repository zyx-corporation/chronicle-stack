# Release Notes v1.157.0

- added a dedicated multi-panel renderer for `/api/ai-index-status` so AI index status opens as a feature-facing route instead of raw response JSON only
- surfaced vector counts, graph counts, index metadata, and read-only boundary flags in the local UI before deeper inspection
- updated operator-facing validation guidance so local UI walkthroughs include AI index status route checks
