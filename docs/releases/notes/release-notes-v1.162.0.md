# Release Notes v1.162.0

- added read-only detail routes for `/api/ai-index/graph-edges/<source>/<relation>/<target>` so graph-edge rows and summary rails can open a concrete detail surface
- surfaced latest graph-edge detail navigation together with source or target graph-node links and neighbor counts in the local UI
- updated operator-facing validation guidance so local UI walkthroughs include graph-edge detail route checks
