# Release Notes v1.161.0

- added a dedicated workspace renderer for `/api/ai-index-graph-edges` so graph edges open with a summary rail instead of the generic fallback table
- surfaced graph-edge counts, relation and property-key breakdowns, and source or target node coverage in the local read-only UI
- updated operator-facing validation guidance so local UI walkthroughs include AI index graph edge route checks
