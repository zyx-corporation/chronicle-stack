# Release Notes v1.114.0

- added an audit-specific route renderer that surfaces `governance_summary` above the audit stream
- added an `Audit Governance` detail notice that surfaces operational implication, boundary/lifecycle links, and impacted target context
- updated UI shell coverage for the audit governance route and detail notices
