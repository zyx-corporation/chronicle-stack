# Release Notes v1.125.0

- added a shared helper for rendering grouped workspace button rows
- aligned runtime, review queue, and summary jobs route controls to compose through the shared row helper
- updated overview panels to use the same grouped button-row path for route-local workspace controls
