# Release Notes v1.159.0

- added a dedicated workspace renderer for `/api/ai-index-vector` so vector entries open with a summary rail instead of the generic fallback table
- surfaced vector-entry counts, embedding provider or model breakdowns, latest indexed detail navigation, and source metadata in the local read-only UI
- updated operator-facing validation guidance so local UI walkthroughs include AI index vector route checks
