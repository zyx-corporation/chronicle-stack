# Release Notes v1.144.0

- moved workspace-table detail controls through a shared stacked detail-cell helper so `JSON` and primary detail navigation stay visible together
- adjusted workspace-table geometry so action-heavy rows can expand to intrinsic width and scroll horizontally instead of collapsing list-level affordances
- updated operator-facing validation guidance so manual local UI review explicitly checks detail affordance reachability across runtime, review, and summary workspaces
