# Release Notes v1.108.0

- added an `overview_evidence_summary` aggregate to `/api/overview` for trust, boundary, audit, lifecycle, auth, and federation inspection cues
- surfaced that aggregate in the local web overview as an Evidence Rail panel with drilldown links into the latest boundary, audit, and lifecycle records
- updated overview and shell tests for the new read-only evidence-priority surface
