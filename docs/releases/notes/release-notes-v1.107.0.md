# Release Notes v1.107.0

- added a `current_work_summary` aggregate to `/api/overview` for current questions, proposal pressure, objections, hypotheses, and artifact candidates
- surfaced that aggregate in the local web overview as a dedicated Current Work panel with detail navigation links
- updated overview and shell tests for the new read-only current-work surface
