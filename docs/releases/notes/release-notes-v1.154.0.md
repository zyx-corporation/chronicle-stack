# Release Notes v1.154.0

- added a dedicated multi-panel renderer for `/api/package-review` so package review snapshots open as a feature-facing route instead of raw response JSON only
- surfaced package review status, counts, warning codes, and finding recommendations in the local read-only UI before deeper review drilldown
- updated operator-facing validation guidance so local UI walkthroughs include package review route checks
