# Release Notes v1.146.0

- added a dedicated `Artifacts Workspace` renderer for `/api/artifacts` so the route opens with a workspace summary rail instead of the generic fallback table
- surfaced artifact list context for status, version history, proposal counts, and source metadata before detail is opened
- verified the local web UI shell and data service now expose the artifacts workspace summary payload and renderer wiring expected by operator validation
