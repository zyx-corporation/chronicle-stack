# Release Notes v1.160.0

- added a dedicated workspace renderer for `/api/ai-index-graph-nodes` so graph nodes open with a summary rail instead of the generic fallback table
- surfaced graph-node counts, label and property-key breakdowns, latest node detail navigation, and source metadata in the local read-only UI
- updated operator-facing validation guidance so local UI walkthroughs include AI index graph node route checks
