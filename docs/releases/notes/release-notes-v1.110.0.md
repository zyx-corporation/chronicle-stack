# Release Notes v1.110.0

- enriched `/api/federation-package-preview` with `package_route_summary`, `trust_reference_summary`, `consent_summary`, and `import_implication_summary`
- made preview and import-preview payloads read more like a shipping inspection desk without adding a new endpoint
- added federation package preview coverage for trust, consent, and import implications in the UI read-model tests
