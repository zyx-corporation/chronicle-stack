# Release Notes v1.126.0

- added a shared route-level workspace table controls helper for filter, button-row, empty-state, and mutation preview composition
- aligned runtime records, review queue, and summary jobs list routes to compose their shared control clusters through the helper
- updated UI shell coverage and release docs for the workspace table controls path
