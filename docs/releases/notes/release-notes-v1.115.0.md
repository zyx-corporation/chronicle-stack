# Release Notes v1.115.0

- added a federation package preview route renderer that surfaces the package review contract above the raw JSON payload
- added dedicated UI panels for package route, trust reference, consent, and import implication summaries on `/api/federation-package-preview`
- updated UI shell coverage for the federation package preview route renderer
