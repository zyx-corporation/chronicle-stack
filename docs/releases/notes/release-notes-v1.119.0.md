# Release Notes v1.119.0

- added a shared multi-panel route helper for the read-only audit, boundary, lifecycle, and federation route renderers
- aligned those route renderers to the same panel composition path without changing payload contracts or navigation behavior
- updated UI shell coverage for the shared route layout helper
