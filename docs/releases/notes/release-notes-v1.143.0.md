# Release Notes v1.143.0

- expanded `Review Queue` target rows so review capability status is visible directly beside the target summary
- surfaced package readiness, auth boundary, CLI parity, mutation preview state, and identity assurance context at list level without changing the underlying payload
- updated operator-facing validation guidance so manual local UI review checks the richer review-queue workspace cues
