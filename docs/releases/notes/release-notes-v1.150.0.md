# Release Notes v1.150.0

- added a dedicated `Reactions Workspace` renderer for `/api/reactions` so reaction records open with a workspace summary rail instead of the generic fallback table
- surfaced reaction list context for reaction type, target linkage, creator, and related resource paths before detail is opened
- updated operator-facing validation guidance so local UI walkthroughs include `Reactions Workspace` route checks
