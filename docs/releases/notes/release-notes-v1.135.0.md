# Release Notes v1.135.0

- documented that review queue, runtime records, and summary jobs rows now expose a shared primary `Open Detail` entry in the first workspace column
- clarified in the CLI reference that the adjacent JSON button remains available for raw payload inspection
- extended the operator validation guide so manual UI checks explicitly verify the shared detail-entry pattern across all three workspaces
