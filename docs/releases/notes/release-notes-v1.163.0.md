# Release Notes v1.163.0

- added a local backup helper that archives `.chronicle/` into timestamped tarballs without mutating Chronicle records
- added a local operator runbook and a backup/restore guide for single-operator local deployment
- linked the new local-operations entry points from the main README and curl-based local deployment guide
