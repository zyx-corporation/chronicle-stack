# Release Notes v1.138.0

- added a shared `Outcome Matrix` detail notice that summarizes per-action disposition, queue state, UI intent, and reviewer-kind expectations for review-backed detail records
- extended review-backed runtime and summary detail surfaces to reuse the same derived outcome matrix summary as review detail
- updated operator-facing docs so the new outcome matrix notice is part of manual local UI validation
