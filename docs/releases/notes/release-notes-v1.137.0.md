# Release Notes v1.137.0

- added a shared `Identity Sufficiency` detail notice that summarizes auth status, assurance status, blockers, and next action for review-backed detail records
- extended review-backed runtime and summary detail surfaces to reuse the same derived identity sufficiency summary as review detail
- updated operator-facing docs so the new identity sufficiency notice is part of manual local UI validation
