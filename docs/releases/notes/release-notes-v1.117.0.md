# Release Notes v1.117.0

- added mirrored governance-oriented summary rails to `/api/boundary` and `/api/lifecycle`
- replaced the generic boundary and lifecycle list rendering with dedicated read-only route renderers
- updated UI shell coverage for the new boundary and lifecycle governance panels
