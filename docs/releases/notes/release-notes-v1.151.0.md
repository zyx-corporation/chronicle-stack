# Release Notes v1.151.0

- added a dedicated `Chronicle Objects Workspace` renderer for `/api/chronicle-objects` so object records open with a workspace summary rail instead of the generic fallback table
- surfaced object list context for object type, lifecycle, visibility, evidence linkage, and derived-vs-explicit state before detail is opened
- updated operator-facing validation guidance so local UI walkthroughs include `Chronicle Objects Workspace` route checks
