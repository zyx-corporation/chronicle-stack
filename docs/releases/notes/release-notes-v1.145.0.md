# Release Notes v1.145.0

- fixed the review-queue detail-path resolver so first-column `JSON` and `Open Detail` buttons render again for pending review rows
- verified in the local browser walkthrough that the first review row now exposes actionable `JSON` and `詳細を開く` controls with stable geometry
- added follow-up operator evidence for the repaired review-queue detail navigation path
