# Release Notes v1.121.0

- added a route-level workspace summary panel to `/api/review-queue`
- exposed `review_queue_summary` directly on the review queue endpoint payload using the existing triage aggregation
- updated UI shell and endpoint coverage for the review queue route summary
