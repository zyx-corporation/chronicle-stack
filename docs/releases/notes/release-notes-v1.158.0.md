# Release Notes v1.158.0

- added a dedicated multi-panel renderer for `/api/ui-boundary` so UI boundary metadata opens as a feature-facing route instead of raw response JSON only
- surfaced mutation state, auth boundary guidance, reviewer-context requirements, and write-route contract cues in the local read-only UI before deeper inspection
- updated operator-facing validation guidance so local UI walkthroughs include UI boundary route checks
