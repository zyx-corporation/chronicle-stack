# Chronicle Stack v1.66.0 Release Notes

## Highlights

- adds a local graph retrieval adapter over the versioned derived graph export
- exposes adapter metadata in `runtime retrieve-plan` and `chronicle graph retrieve`
- keeps retrieval dry-runs local-first, read-only, and non-GraphRAG-runtime
