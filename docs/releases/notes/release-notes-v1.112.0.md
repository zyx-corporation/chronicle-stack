# Release Notes v1.112.0

- enriched `/api/runtime-records` with `posture_role`, `downstream_boundary_note`, `trial_sufficiency_summary`, and `handoff_summary`
- enriched `/api/summary-jobs` with concise `package_readiness_summary`, `auth_advisory_summary`, and `identity_assurance_summary`
- aligned runtime and summary-job detail payloads with the new workspace-oriented read-model summaries
