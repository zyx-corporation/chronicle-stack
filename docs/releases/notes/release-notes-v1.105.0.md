# Release Notes v1.105.0

- expanded `/api/artifacts/<id>` with workbench-ready linked context, decision, RDE, source-event, boundary, and audit summaries
- kept the artifact detail surface read-only so the new workbench summaries remain derived navigation aids rather than mutation authority
- updated UI tests and release docs for the richer artifact detail contract
