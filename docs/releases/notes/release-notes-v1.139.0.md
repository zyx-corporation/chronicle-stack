# Release Notes v1.139.0

- added a shared `Apply Prerequisites` detail notice that summarizes package context, reviewer boundary readiness, execution-path status, and suggested commands for review-backed detail records
- extended review-backed runtime and summary detail surfaces to reuse the same derived apply prerequisites summary as review detail
- updated operator-facing docs so the new apply prerequisites notice is part of manual local UI validation
