# Release Notes v1.149.0

- added a dedicated `Events Workspace` renderer for `/api/events` so recent Chronicle events open with a workspace summary rail instead of the generic fallback table
- surfaced event list context for event type, actor, review status, and linked record references before detail is opened
- updated operator-facing validation guidance so local UI walkthroughs include `Events Workspace` route checks
