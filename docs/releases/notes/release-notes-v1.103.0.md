# Release Notes v1.103.0

- added a manual `chronicle federation package verify --package-dir <package_dir> --json` template to the overview Federation panel
- kept the panel read-only so it continues to guide local review without executing package verification operations
- updated tests, README, and release docs for the new overview template surface
