# Release Notes v1.152.0

- added a dedicated `RDE Workspace` renderer for `/api/rde` so diff records open with a workspace summary rail instead of the generic fallback table
- surfaced RDE list context for artifact linkage, version transition, unresolved counts, deviation risks, and next-update policy before detail is opened
- updated operator-facing validation guidance so local UI walkthroughs include `RDE Workspace` route checks
