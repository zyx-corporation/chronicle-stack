# Release Notes v1.101.0

- added a manual `chronicle federation package import-preview --package-dir <package_dir> --json` template to the overview Federation panel
- kept the panel read-only so it continues to guide local review without executing package import operations
- updated tests, README, and release docs for the new overview template surface
