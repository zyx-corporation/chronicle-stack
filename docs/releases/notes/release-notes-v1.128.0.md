# Release Notes v1.128.0

- added shared workspace table query helpers for reviewer-boundary filtering and repeated response metadata token matching
- aligned runtime records, review queue, and summary jobs list filters to compose their query predicates through the shared helpers
- updated UI shell coverage and release docs for the workspace query helper path
