# Release Notes v1.131.0

- added a shared workspace summary panel helper for repeated workspace panel composition
- aligned runtime records, review queue, and summary jobs workspace panels to compose their counts, summary rails, and latest-response links through the helper
- updated UI shell coverage and release docs for the workspace summary panel path
