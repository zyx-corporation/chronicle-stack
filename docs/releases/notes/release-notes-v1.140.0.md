# Release Notes v1.140.0

- added dedicated `Trust Workspace` route renderers for `Trust Nodes` and `Trust Relations` so node coverage, relation state, purpose, capability, and history can be read without falling back to generic tables
- exposed shared `trust_overview` aggregates on trust route payloads to support the new workspace summary rail
- updated operator-facing docs so Trust Workspace route validation is part of manual local UI checks
