# Release Notes v1.129.0

- added a shared review attention ranking helper for repeated workspace sort priority logic
- aligned review queue and summary jobs ranking paths to compose their shared drift/advisory/ready/package/resolved ordering through the helper
- updated UI shell coverage and release docs for the shared ranking path
