# Release Notes v1.148.0

- added a dedicated `Contexts Workspace` renderer for `/api/contexts` so context records open with a workspace summary rail instead of the generic fallback table
- surfaced context list context for scope, visibility, proposal counts, and source metadata before detail is opened
- updated operator-facing validation guidance so local UI walkthroughs include `Contexts Workspace` route checks
