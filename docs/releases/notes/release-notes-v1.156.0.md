# Release Notes v1.156.0

- added a dedicated multi-panel renderer for `/api/graph-summary` so graph summary opens as a feature-facing route instead of raw response JSON only
- surfaced graph counts, incremental expectations, and read-only boundary flags in the local UI before deeper drilldown
- updated operator-facing validation guidance so local UI walkthroughs include graph summary route checks
