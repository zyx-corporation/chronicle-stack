# Release Notes v1.164.0

- added a local restore helper that can rehydrate `.chronicle/` from a backup archive while preserving the current state by default
- updated the local backup/restore guide to prefer helper-based restore while still documenting the manual path
- extended the local operator runbook with a direct recovery shortcut for restoring a known-good Chronicle snapshot
