# Release Notes v1.120.0

- added route-level workspace summary panels to `/api/runtime-records` and `/api/summary-jobs`
- exposed `runtime_records_summary` and `summary_jobs_summary` directly on those endpoint payloads using the existing overview aggregations
- updated UI shell and endpoint coverage for the runtime workspace route summaries
