# Release Notes v1.147.0

- added a dedicated `Decisions Workspace` renderer for `/api/decisions` so decision records open with a workspace summary rail instead of the generic fallback table
- surfaced decision list context for decision type, actor, target linkage, and latest decision timing before detail is opened
- updated operator-facing validation guidance so local UI walkthroughs include `Decisions Workspace` route checks
