# Chronicle Stack v1.51.0 Release Notes

Chronicle Stack `v1.51.0` is a local package-handoff command-detail rendering release over the published `v1.50.0` baseline.

## Added

- structured suggested-command details for package-handoff preview payloads
- key-first rendering for package-handoff suggested command lines
- regression coverage for package-handoff command-detail payloads and renderers

## Kept stable

- local-first read-only package handoff preview semantics
- append-only Chronicle record authority
- copyable suggested CLI commands and package-handoff boundary wording
