# Release Notes v1.130.0

- added a shared runtime readiness comparator for repeated mutation/auth workspace sorting
- aligned runtime record mutation-sort and auth-sort paths to compose their shared readiness ordering through the helper
- updated UI shell coverage and release docs for the runtime ranking helper path
