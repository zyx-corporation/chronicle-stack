# Release Notes v1.109.0

- enriched `/api/trust-nodes` with latest activity and domain coverage summaries so node and subject relationships read as a single local trust surface
- enriched `/api/trust-relations` with subject, active-state, audit-history, and federation-implication summaries for read-only Trust Workspace detail flows
- added trust payload coverage for withdrawn relations and advisory federation implications
