# Release Notes v1.134.0

- added a primary `Open Detail` entry button to review queue rows so operators can open row detail directly from the first workspace column
- aligned runtime records and summary jobs with the same primary detail-entry affordance to keep workspace table navigation consistent
- tightened the UI shell release trail so the shared detail-entry pattern is now required across all three workspace tables
