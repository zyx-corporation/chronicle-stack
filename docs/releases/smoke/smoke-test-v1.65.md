# Chronicle Stack v1.65 Smoke Test

## Graph export contract

- confirm graph export includes `export_contract.contract_version`
- confirm graph CLI summary returns `contract_version` and `incremental_mode`
- confirm `/api/graph-summary` returns `contract_version` and `incremental_expectations`
