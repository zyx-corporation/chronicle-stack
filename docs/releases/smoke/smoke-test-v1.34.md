# Chronicle Stack v1.34.0 Smoke Test

Expected `v1.34.0` contract additions:

- review-action preview summaries expose `recovery_summary_key`
- review-action preview summaries expose `follow_up_summary_key`
- preview-shell rendering prefers structured preview summary keys
