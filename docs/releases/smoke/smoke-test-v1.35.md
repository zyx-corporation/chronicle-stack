# Chronicle Stack v1.35.0 Smoke Test

Expected `v1.35.0` contract additions:

- review write-route `failure_families` expose `summary_key`
- nested review-action `failure_contract.failure_families` expose `summary_key`
- contract rendering prefers structured failure-family summaries
