# Chronicle Stack v1.32.0 Smoke Test

Expected `v1.32.0` contract additions:

- blocked/error review-action payloads expose `message_key`
- review-action failure summaries expose `failure_summary_key` when present
- review-action result rendering prefers structured failure keys
