# Chronicle Stack v1.30.0 Smoke Test

Expected `v1.30.0` contract additions:

- runtime-record, review-queue, and summary-job mutation summaries expose `message_key`
- runtime-record, review-queue, and summary-job mutation summaries expose `scope_note_key`
- runtime-record, review-queue, and summary-job mutation summaries expose `remaining_summary_key` when prerequisites remain
